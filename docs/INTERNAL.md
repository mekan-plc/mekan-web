# Mekan Web (mekan-web) — Internal Documentation

This repo uses a **single master internal documentation** located in the WordPress repository:

- `mekan-cms/docs/INTERNAL.md`

Reason:
- Avoid duplicated docs drifting over time.
- Keep the full system overview (CMS + Web) in one place.

If you are reading this file inside `mekan-web`, open the master doc in the `mekan-cms` repo.
